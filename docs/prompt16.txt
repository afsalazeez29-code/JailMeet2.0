JailMeet 2.0 — Visitor Registration Page Migration (Analysis + Implementation)

Project root:
C:\Users\afsal\OneDrive\Desktop\JailMeet2.0

Frontend root:
C:\Users\afsal\OneDrive\Desktop\JailMeet2.0\frontend

Backend root:
C:\Users\afsal\OneDrive\Desktop\JailMeet2.0\backend

Old source file to analyze and migrate:
C:\Users\afsal\OneDrive\Desktop\JailMeet2.0\register.php

Current state (already working, do not touch):
- Backend fully working: auth, RBAC, dashboard APIs, visitor registration API (POST /api/auth/register-visitor) — all tested and confirmed working
- Frontend: landing page, /login, and all four role dashboards (Admin, Visitor, Officer, Prisoner) migrated and working
- Shared architecture foundation exists (or is being built): frontend/src/types/, frontend/src/services/ (auth.service.ts, dashboard.service.ts already created in a prior task)
- Active components currently live under frontend/components — do not move everything into frontend/src/components during this task

Goal:
Migrate the old visitor registration page (register.php) to Next.js, preserving the old UI/UX (form fields, layout, visual style) while replacing all PHP/MySQL logic with the existing modern backend, connected via a typed service layer.

==================================================
STEP 1 — ANALYZE THE OLD REGISTER.PHP FILE (read-only, no changes)
==================================================

Read and analyze:
C:\Users\afsal\OneDrive\Desktop\JailMeet2.0\register.php

Document, before writing any code:
1. All visible form fields and their old PHP $_POST names (expected: username, email, phone, password, district — confirm exact names from the actual file)
2. Old validation logic (e.g. phone number regex, required fields, client-side JS validation if present)
3. Old database interaction: which table (visitors), which columns (vname, vemail, vphno, vpass, vstate, vadd, vzip, profile_pic, etc. — confirm exact columns from the actual file)
4. Confirm the old code stores the password in plaintext (no hashing) — flag this explicitly as unsafe and NOT to be replicated
5. UI structure: hero section, Bootstrap classes, inline styles, AOS attributes, background image, success popup mechanism, login link, any newsletter/footer content that is unrelated to registration and should be excluded
6. Old success/redirect flow (redirects to login.php after successful registration)
7. Any old vendor scripts loaded (main.js, preloader, GLightbox, Swiper, Isotope, etc.) — these should NOT be migrated since they aren't used by the registration form itself

==================================================
STEP 2 — INSPECT THE REAL BACKEND CONTRACT (read-only, no changes)
==================================================

Before writing any frontend code, inspect the actual backend registration endpoint to get the EXACT field names and response shape. Do not guess or assume field names match the old PHP form.

Inspect:
backend/src/modules/auth/auth.routes.ts
backend/src/modules/auth/auth.validation.ts
backend/src/modules/auth/auth.controller.ts
backend/src/modules/auth/auth.service.ts

Confirm:
1. The exact Zod schema field names expected by POST /api/auth/register-visitor (likely name, email, password, phone, address, state, zip — but verify against actual code, do not assume)
2. Required vs optional fields
3. The exact success response shape (e.g. { success, message, data: { user: {...}, visitorProfile: {...} } })
4. The exact error response shape for validation errors and duplicate-email conflicts (likely 400 and 409 respectively)
5. Any password strength/length constraints enforced by Zod

Report both Step 1 and Step 2 findings clearly before proceeding to implementation, in a short summary (not a separate doc file — inline in your response is fine), specifically calling out any mismatch between the old PHP field names and the real backend field names.

==================================================
STEP 3 — CREATE/UPDATE SHARED TYPES
==================================================

Update or create:
frontend/src/types/auth.ts

Add (or extend existing types with) a typed shape for:
1. The visitor registration request payload — using the EXACT field names confirmed in Step 2, not the old PHP names
2. The visitor registration success response
3. The visitor registration error response (validation error shape and duplicate-email conflict shape)

Do not duplicate or conflict with any existing types already in auth.ts from prior work (login types, current-user types).

==================================================
STEP 4 — EXTEND THE AUTH SERVICE
==================================================

Update:
frontend/src/services/auth.service.ts

Add a typed function, e.g. registerVisitor(payload), that:
1. Calls POST ${NEXT_PUBLIC_API_BASE_URL}/auth/register-visitor with the exact backend field names
2. Sends the payload as JSON with correct headers
3. Returns structured typed success data, or throws a predictable typed API error (reusing the same error-handling pattern already established in this service file for consistency — do not invent a new pattern)
4. Does NOT redirect, does NOT touch React router, does NOT hold UI state — same rules as the existing service functions
5. Does NOT store any token on registration (registration does not log the user in automatically — they are redirected to /login to sign in fresh, matching the old flow)

==================================================
STEP 5 — BUILD THE REGISTRATION UI
==================================================

Create this file structure:

frontend/app/register/page.tsx
frontend/components/auth/VisitorRegisterForm.tsx
frontend/components/auth/VisitorRegisterForm.module.css

app/register/page.tsx should be a minimal route file:

import VisitorRegisterForm from "@/components/auth/VisitorRegisterForm";

export default function RegisterPage() {
  return <VisitorRegisterForm />;
}

VisitorRegisterForm.tsx requirements:
1. "use client" component (uses useState, form submission, router redirect)
2. Form fields matching the old UI layout/labels but using the REAL backend field names for state (confirmed in Step 2) — e.g. if the backend expects "name" not "username", the input can still display the label "Username" or "Full Name" (matching old UI text) while the underlying state key is "name"
3. React state for: each form field, loading, error (string | null), success (boolean or message string)
4. Basic client-side validation before submit (required fields, phone format) — this is a UX nicety, NOT a replacement for backend Zod validation; backend validation is still the source of truth
5. On submit:
   - Prevent default, clear previous error, set loading true
   - Call registerVisitor() from auth.service.ts with the exact backend field names
   - On validation error (400): display the backend's error message(s) inline
   - On duplicate email (409): display a clear "Email already registered" message
   - On success: show a success message (matching old UI's success popup style/feel), then redirect to /login after a short delay (or immediately — match old UX intent, e.g. old page auto-redirected to login.php after success)
   - Always set loading false when the request settles
6. Disable the submit button while loading, show a loading indicator
7. NEVER handle passwords insecurely — no plaintext storage, no logging passwords, password only exists in component state and is sent over HTTPS-bound fetch to the backend for hashing server-side (this already happens correctly in the backend; the frontend just must not do anything unsafe with it either, like storing it in localStorage or console.logging it)

Preserve visually from the old register.php:
1. Hero section layout, container structure, form width, spacing
2. Bootstrap classes already used elsewhere in the migrated project (hero, section, dark-background, container, form-control, btn, btn-primary) — ONLY if the registration page loads the required legacy CSS locally, scoped to this page (do not add landing CSS back to the global root layout: frontend/app/layout.tsx must remain untouched)
3. Background image if one exists and is already available under public/legacy — if not found, note it as missing rather than guessing a substitute
4. Success message style/feel (adapted to React state instead of a JS alert/popup)
5. Login link at the bottom, pointing to /login

Use VisitorRegisterForm.module.css for registration-specific styles (form width, success/error message styling, password field spacing, mobile responsiveness) — CSS Modules keep these styles scoped to this component only, preventing any bleed into other pages.

Explicitly DO NOT migrate:
- include('db.php') or any PHP/MySQL logic
- $_POST handling or real_escape_string
- Plaintext password storage
- The old <form action="register.php"> submission mechanism
- The old newsletter form or template footer content unrelated to registration
- Old vendor scripts: main.js, preloader, GLightbox, Swiper, Isotope, or any other legacy JS library not directly required by this form

==================================================
STEP 6 — VERIFICATION
==================================================

Run from frontend:
npm run typecheck
npm run build

Both must pass with zero errors.

Manually confirm the following behaviors are correctly implemented (describe how each is handled in your report, do not just claim it works):
1. Submitting valid data → success message shown → redirect to /login
2. Submitting a duplicate email → clear "already registered" error shown, no crash
3. Submitting invalid data (e.g. missing password) → backend validation error displayed clearly
4. Network/API failure → visible error state, not a blank screen or unhandled crash
5. Password field never appears in plaintext anywhere outside the form's own state (no console.log, no localStorage, no URL params)

==================================================
STRICT NON-GOALS
==================================================

Do not do any of the following in this task:
- Do not modify the backend in any way (routes, controllers, services, Zod schemas, Prisma schema)
- Do not modify any of the four dashboard pages
- Do not add landing CSS back into the global app/layout.tsx
- Do not move existing components from frontend/components into frontend/src/components
- Do not create hooks (useAuth, etc.) in this task
- Do not implement visitor login changes — /login already works and is untouched
- Do not implement visitor booking, status, or profile pages — registration only
- Do not delete or modify register.php or any other old PHP file
- Do not run npm audit fix --force

==================================================
FINAL REPORT
==================================================

After completing all steps, report:
1. Old register.php field names and structure found (Step 1 summary)
2. Real backend field names and response shapes found (Step 2 summary), and any mismatch with the old PHP field names
3. Files created/modified (types, service, page, component, CSS module)
4. Exact request payload shape sent to POST /api/auth/register-visitor
5. Confirmation of success/error/duplicate-email/network-error handling behavior
6. Assets used (background image, legacy CSS classes) and any missing assets found
7. Confirmation that landing CSS was NOT added to the global layout
8. npm run typecheck result
9. npm run build result
10. Any risks or follow-up items (e.g. backend field naming that differs from old UI expectations, missing background image, etc.)