JailMeet 2.0 — Admin Dashboard Migration (Full Workflow)

Project: C:\Users\afsal\OneDrive\Desktop\JailMeet2.0

Current state (already working, do not touch):
- Backend fully working: auth, RBAC, dashboard APIs (GET /api/dashboard/admin, /officer, /visitor, /prisoner), visitor registration — all tested
- Frontend: Next.js scaffold working, landing page converted, /login page converted and confirmed visible/working with role-based redirect
- Legacy assets already copied to frontend/public/legacy
- Seeded test accounts exist for all 4 roles

Overall goal for this task:
Migrate the OLD admin dashboard (PHP) to Next.js, preserving old UI/UX exactly, while removing all PHP/session/database logic and connecting it to the existing backend API. This must be done in THREE strict phases, in order. Do NOT skip a phase, do NOT merge phases, and STOP after Phase 1 to let me review the plan before writing any code.

============================================================
PHASE 1 — ANALYSIS ONLY (no code changes)
============================================================

Analyze the old admin dashboard and its layout files. Do NOT modify any files in this phase.

Source files to inspect:
C:\Users\afsal\OneDrive\Desktop\JailMeet2.0\frontend\admin\adindex.php
C:\Users\afsal\OneDrive\Desktop\JailMeet2.0\frontend\admin\includes
(and any header/navbar/sidebar/footer include files referenced from adindex.php, even if not in the includes folder — trace every include()/require() call)

Create a new document at:
docs/10-admin-dashboard-ui-migration-plan.md

The document must include ALL of the following sections:
1. Old layout files used (full list with paths)
2. Old CSS/JS assets required (full list with paths, note which are already copied to public/legacy and which are missing)
3. Images/icons required (full list, note missing ones without guessing substitutes)
4. Sidebar/navbar/header/footer structure — describe the HTML structure and how these pieces are assembled/included in adindex.php
5. PHP code to remove — list every PHP block, session check, mysqli query, include/require, and redirect found
6. Dynamic dashboard data — list every piece of dynamic data displayed (counts, tables, names, etc.) and where in the old code it comes from
7. Matching backend API fields — cross-reference the dynamic data from #6 against the actual response shape of GET /api/dashboard/admin (fetch/inspect it if needed) and flag any old dashboard data point that has NO matching backend field yet
8. New component breakdown — proposed component list (e.g. AdminLayout, AdminNavbar, AdminSidebar, AdminFooter, dashboard content sections)
9. New route structure — proposed Next.js route(s)
10. Risks and missing assets — anything that could block a faithful conversion
11. Exact next coding step — a one-paragraph summary of what Phase 2 should do

After Phase 1, STOP. Show me the full content of docs/10-admin-dashboard-ui-migration-plan.md and wait for my explicit confirmation before proceeding to Phase 2. Do not write any component code yet.

============================================================
PHASE 2 — CONVERT ADMIN LAYOUT SHELL (only after I confirm Phase 1)
============================================================

Convert only the old admin layout shell (navbar, sidebar, footer, page wrapper) into reusable Next.js components. Do NOT convert dashboard content/data in this phase.

Source files (adjust based on what Phase 1 analysis actually found):
- frontend/admin/adindex.php
- frontend/admin/includes/header.php
- frontend/admin/includes/navbar.php
- frontend/admin/includes/sidebar.php
- frontend/admin/includes/footer.php

Target files:
- components/legacy/admin/AdminLayout.tsx
- components/legacy/admin/AdminNavbar.tsx
- components/legacy/admin/AdminSidebar.tsx
- components/legacy/admin/AdminFooter.tsx
- app/admin/layout.tsx (wires AdminLayout around all /admin/* routes)

Rules:
1. Convert only the layout/shell — no dashboard stats, no data fetching yet.
2. Remove all PHP includes, session checks, and PHP logic.
3. Convert HTML to valid JSX (class→className, for→htmlFor, self-close void elements, fix inline styles).
4. Preserve old classes, structure, spacing, and visual hierarchy exactly — this is a technology conversion, not a redesign.
5. Fix asset paths to /legacy/admin/... (never /public/legacy/...). If Phase 1 flagged missing assets, skip them gracefully (no broken image icons) and list them again in your report — do not block the build over missing assets.
6. If the old sidebar/navbar has active-link highlighting or a mobile toggle, reimplement with React state/usePathname from next/navigation rather than vanilla DOM JS. Mark components "use client" only where such interactivity requires it.
7. Do not convert dashboard page content yet — app/admin/dashboard/page.tsx (if created) should just render a placeholder inside AdminLayout for now, e.g. <div>Admin dashboard content coming in Phase 3</div>.
8. Do not modify the backend.
9. Do not touch /login, landing page, or any other already-converted page.
10. Run:
    npm run typecheck
    npm run build
    Both must pass with zero errors.

Report after Phase 2:
- Files created/modified
- Assets referenced and confirmed present vs missing
- Any interactivity reimplemented and how
- npm run typecheck result
- npm run build result

Then proceed directly to Phase 3 — no need to stop again, unless Phase 2 build fails, in which case stop and report the failure.

============================================================
PHASE 3 — CONVERT ADMIN DASHBOARD CONTENT
============================================================

Convert only the old admin dashboard's data/content into Next.js, using the AdminLayout built in Phase 2, and connect it to the real backend API.

Source:
frontend/admin/adindex.php (content area only — layout already handled in Phase 2)

Target:
app/admin/dashboard/page.tsx

Backend API:
GET http://localhost:5000/api/dashboard/admin
(Protected route — requires Authorization: Bearer <token> header)

Rules:
1. Use the existing AdminLayout component from Phase 2 — do not duplicate navbar/sidebar/footer markup here.
2. Remove all PHP/MySQL logic from the old dashboard content.
3. This must be a client component (or use a client sub-component) since it fetches data with the stored JWT token.
4. Retrieve the access token using the same auth token helper/localStorage key established during the /login page work — do not invent a second storage mechanism.
5. Fetch stats from GET /api/dashboard/admin on mount and display:
   totalUsers, totalVisitors, totalOfficers, totalPrisoners, totalAppointments, pendingAppointments, pendingParoleRequests
   (Adjust exactly to whatever fields Phase 1's analysis confirmed the API actually returns.)
6. Keep the old card/stat-box layout, classes, and visual style as closely as possible — same grid, same icons, same colors, same wording, just wired to real data.
7. If the backend response is missing an expected field, display 0 for that stat instead of crashing or showing "undefined."
8. If no access token is found in storage, redirect to /login immediately (do not attempt the fetch).
9. If the API call returns 401, clear the stored token and redirect to /login.
10. If the API call returns 403 ("Forbidden"), show a clear "Access denied" message instead of attempting to render dashboard content.
11. Show a simple loading state while the fetch is in progress (don't leave a blank screen).
12. Do not convert any other page (officer/visitor/prisoner dashboards, or any deeper admin pages like user management) in this task — admin dashboard summary view only.
13. Do not modify the backend.
14. Run:
    npm run typecheck
    npm run build
    Both must pass with zero errors.

Final report after Phase 3 (and for the whole task):
- Full list of files created/modified across Phase 2 and Phase 3
- Confirmation of which stat fields were successfully wired vs defaulted to 0
- Confirmation of token/401/403/loading handling behavior
- Any assets still missing
- Final npm run typecheck result
- Final npm run build result