You are working on the JailMeet 2.0 Next.js frontend.

Project root:
C:\Users\afsal\OneDrive\Desktop\JailMeet2.0

Frontend root:
C:\Users\afsal\OneDrive\Desktop\JailMeet2.0\frontend

Current status:
- Visitor registration page now exists at:
  frontend/app/register/page.tsx
- Registration form component exists at:
  frontend/components/auth/VisitorRegisterForm.tsx
- The form is connected to:
  POST /api/auth/register-visitor
- Valid registration redirects to:
  /login
- Typecheck and build currently pass.
- Some active UI links may still point to old PHP registration paths such as:
  register.php
  visitor/register.php

Goal:
Find every active frontend registration link that still points to an old PHP file and replace it with the valid Next.js route:

/register

Do not redesign anything.
Do not modify backend code.
Do not change registration form behavior.
Do not change login behavior.
Do not create new pages.

==================================================
FILES TO INSPECT
==================================================

Inspect these files first:

- frontend/app/page.tsx
- frontend/app/login/page.tsx
- frontend/app/register/page.tsx
- frontend/components/auth/LoginForm.tsx
- frontend/components/auth/VisitorRegisterForm.tsx
- frontend/components/legacy/landing/
- frontend/components/legacy/
- frontend/public/legacy/landing/

Also search the complete active frontend source for:

- register.php
- visitor/register.php
- ./register.php
- ../register.php
- /register.php
- signup.php
- registration.php
- Create Account
- Register
- Sign Up
- legacyHref
- data-legacy-href

Search active source files including:

- .tsx
- .ts
- .jsx
- .js
- .html

Do not modify:

- frontend/legacy-reference/
- docs/
- old PHP reference files
- vendor libraries
- generated .next files
- node_modules

==================================================
ROUTE MAPPING
==================================================

Replace all active old registration routes using this mapping:

register.php
→ /register

visitor/register.php
→ /register

./register.php
→ /register

../register.php
→ /register

/register.php
→ /register

Any other clearly equivalent visitor registration PHP path
→ /register

Do not invent mappings for unrelated PHP pages.

==================================================
NEXT.JS LINK RULES
==================================================

For internal navigation in React components, use:

import Link from "next/link";

Example:

<Link href="/register">Register</Link>

Rules:

1. Replace active registration anchors or navigation handlers with Next.js Link where appropriate.
2. Preserve:
   - existing text
   - Bootstrap classes
   - icons
   - nested elements
   - responsive behavior
   - visual layout
3. Do not use window.location for normal internal navigation.
4. Do not change external links.
5. Do not change hash links.
6. Do not change mailto:, tel:, or download links.
7. Do not add query parameters.
8. Do not create separate role-specific registration routes.
9. All visitor registration links should point to /register.
10. Do not change form submission logic inside VisitorRegisterForm.

==================================================
LOGIN PAGE UPDATE
==================================================

Inspect:

frontend/components/auth/LoginForm.tsx

If the register/create-account link still points to:

register.php

replace it with:

/register

Use Next.js Link while preserving existing classes and appearance.

Expected flow:

/login
→ click Register or Create Account
→ /register

Do not change:

- login API request
- token storage
- role-based redirect
- loading state
- error state
- password handling

==================================================
LANDING PAGE UPDATE
==================================================

Inspect:

frontend/app/page.tsx

Update all active landing-page registration buttons or links to:

/register

Examples may include:

- Register
- Visitor Register
- Create Account
- Get Started
- Sign Up

Only update a button when its current purpose is clearly visitor registration.

Do not change:

- login links already mapped to /login
- navigation design
- CSS classes
- images
- AOS behavior
- landing assets
- section anchors

==================================================
LEGACY METADATA RULE
==================================================

Some components may contain:

- legacyHref
- data-legacy-href

If these are only historical metadata and do not control active navigation:

- leave them unchanged
- report them as inactive metadata

If they are actually used to navigate:

- update the active route to /register
- preserve historical metadata only if useful

Do not mass-edit all .php text blindly.

==================================================
STATIC VERIFICATION
==================================================

After changes, search active frontend source again and confirm there are no active navigation URLs pointing to:

- register.php
- visitor/register.php
- signup.php
- registration.php

Any remaining matches must be classified as:

1. historical metadata
2. comments
3. legacy-reference files
4. vendor/template code
5. unresolved active link

There should be no unresolved active registration links.

==================================================
BROWSER BEHAVIOR
==================================================

Expected behavior:

1. Landing Register button opens:
   /register

2. Login page Register/Create Account link opens:
   /register

3. No registration button opens:
   register.php

4. No registration button opens:
   visitor/register.php

5. /register displays the existing VisitorRegisterForm.

6. Valid registration still:
   - calls POST /api/auth/register-visitor
   - shows success message
   - redirects to /login

7. Duplicate email and validation errors remain unchanged.

==================================================
STRICT NON-GOALS
==================================================

Do not:

- Redesign landing page
- Redesign login page
- Redesign register page
- Change CSS architecture
- Move components
- Change VisitorRegisterForm fields
- Add address or zip fields
- Modify auth.service.ts unless required for a compile fix
- Modify backend
- Modify Prisma
- Add appointment workflow
- Add role-specific registration
- Change JWT/localStorage behavior
- Add cookies
- Add Axios
- Delete legacy files
- Run npm audit fix --force
- Perform unrelated cleanup

==================================================
VERIFICATION COMMANDS
==================================================

Run from:

C:\Users\afsal\OneDrive\Desktop\JailMeet2.0\frontend

Commands:

npm run typecheck
npm run build

If build encounters only the known Windows spawn EPERM issue, rerun the same build in the approved environment without changing code.

==================================================
FINAL REPORT
==================================================

Report:

1. Files inspected
2. Files modified
3. Every old registration route replaced
4. Final Next.js route used
5. Confirmation that Login page Register link opens /register
6. Confirmation that Landing page Register link opens /register
7. Remaining .php matches and why they are inactive
8. Confirmation that UI/classes/layout were unchanged
9. Typecheck result
10. Build result

Stop after completing this registration-link cleanup.

Do not continue to appointment workflow or any other feature without explicit confirmation.