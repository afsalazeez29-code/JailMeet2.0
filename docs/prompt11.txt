You are working on the JailMeet 2.0 frontend architecture cleanup.

Project root:
C:\Users\afsal\OneDrive\Desktop\JailMeet2.0

Frontend root:
C:\Users\afsal\OneDrive\Desktop\JailMeet2.0\frontend

Current status:
- Shared frontend API types and services are already created.
- Visitor Dashboard has already been refactored successfully to use the shared services.
- Typecheck and build currently pass.
- Admin, Officer, and Prisoner dashboards still use local fetch logic.
- Existing UI/UX and behavior must remain unchanged.

Read and inspect these existing files before modifying code:

- frontend/src/types/api.ts
- frontend/src/types/auth.ts
- frontend/src/types/dashboard.ts
- frontend/src/services/auth.service.ts
- frontend/src/services/dashboard.service.ts
- frontend/app/visitor/dashboard/page.tsx
- frontend/app/admin/dashboard/page.tsx
- frontend/app/officer/dashboard/page.tsx
- frontend/app/prisoner/dashboard/page.tsx

Goal:
Refactor the remaining Admin, Officer, and Prisoner dashboards to use the shared auth and dashboard services.

Do not redesign the UI.
Do not modify backend code.
Do not create shared hooks yet.
Do not change CSS loading.
Do not move components.
Do not add new workflow pages.

==================================================
PHASE 1 — ADMIN DASHBOARD SERVICE REFACTOR
==================================================

Modify only:

C:\Users\afsal\OneDrive\Desktop\JailMeet2.0\frontend\app\admin\dashboard\page.tsx

Replace local direct fetch logic with:

- getCurrentUser() from auth.service.ts
- getAdminDashboard() from dashboard.service.ts

Preserve exactly:

- Existing Admin layout
- Existing Kaiadmin UI
- Existing dashboard cards
- Existing CSS classes
- Existing assets
- Existing route behavior
- No-token redirect to /login
- 401 token clear and redirect to /login
- 403 Access denied state
- Loading state
- Visible network/API error state
- Missing stat values defaulting to 0

Use these exact Admin fields:

- totalUsers
- totalVisitors
- totalOfficers
- totalPrisoners
- totalAppointments
- pendingAppointments
- pendingParoleRequests

Do not change field names.
Do not invent additional fields.

After completing Admin refactor, run:

npm run typecheck
npm run build

If verification fails, fix only issues caused by this refactor.

==================================================
PHASE 2 — OFFICER DASHBOARD SERVICE REFACTOR
==================================================

After Admin passes, modify only:

C:\Users\afsal\OneDrive\Desktop\JailMeet2.0\frontend\app\officer\dashboard\page.tsx

Replace local direct fetch logic with:

- getCurrentUser() from auth.service.ts
- getOfficerDashboard() from dashboard.service.ts

Preserve exactly:

- Existing Officer layout
- Existing DeskApp UI
- Existing cards/classes
- Existing route behavior
- No-token redirect to /login
- 401 token clear and redirect to /login
- 403 Access denied state
- Loading state
- Visible network/API error state
- Missing stat values defaulting to 0

Use these exact Officer fields:

- totalPrisoners
- pendingAppointments
- approvedAppointments
- rejectedAppointments
- pendingParoleRequests

Use approvedAppointments exactly.
Do not use acceptedAppointments.

After completing Officer refactor, run:

npm run typecheck
npm run build

If verification fails, fix only issues caused by this refactor.

==================================================
PHASE 3 — PRISONER DASHBOARD SERVICE REFACTOR
==================================================

After Officer passes, modify only:

C:\Users\afsal\OneDrive\Desktop\JailMeet2.0\frontend\app\prisoner\dashboard\page.tsx

Replace local direct fetch logic with:

- getCurrentUser() from auth.service.ts
- getPrisonerDashboard() from dashboard.service.ts

Preserve exactly:

- Existing Prisoner layout
- Existing Dashtreme UI
- Existing card/table classes
- Existing identity display
- Existing route behavior
- No-token redirect to /login
- 401 token clear and redirect to /login
- 403 Access denied state
- Loading state
- Visible network/API error state
- Missing stat values defaulting to 0

Use these exact Prisoner dashboard fields:

- myParoleRequests
- pendingParoleRequests
- approvedParoleRequests
- rejectedParoleRequests
- myAppointments

Use approvedParoleRequests exactly.
Do not use acceptedParoleRequests.

Preserve current identity fields from /auth/me if already displayed:

- id
- name
- email
- role

Do not invent unavailable prisoner profile data such as:

- age
- crime
- jail name
- cell block
- FIR details
- medical details
- parole end date
- visitor names or dates

After completing Prisoner refactor, run:

npm run typecheck
npm run build

If verification fails, fix only issues caused by this refactor.

==================================================
SHARED ERROR-HANDLING RULES
==================================================

Use the existing typed API error behavior from the shared services.

For all three dashboards:

1. No stored token:
   - redirect to /login

2. API status 401:
   - clear the stored token using the existing auth helper
   - redirect to /login

3. API status 403:
   - show Access denied

4. Other API/network errors:
   - show the existing visible error state

5. Do not redirect inside service files.
6. Do not duplicate API base URL logic.
7. Do not duplicate the localStorage token key.
8. Do not add Axios unless already required by the existing architecture.
9. Do not silently swallow errors.

==================================================
STRICT NON-GOALS
==================================================

Do not:

- Refactor Visitor Dashboard again unless required to fix a shared typing issue.
- Create useAuth, useProtectedPage, or useDashboard hooks yet.
- Move components into src/components.
- Change global landing CSS.
- Change role-specific legacy CSS.
- Redesign dashboards.
- Change routes.
- Modify backend.
- Modify Prisma schema, migrations, or seed.
- Change JWT/localStorage strategy.
- Add cookies.
- Add registration, booking, parole, or appointment pages.
- Delete legacy assets or PHP reference files.
- Run npm audit fix --force.

==================================================
FINAL VERIFICATION
==================================================

After all three dashboard refactors, run from:

C:\Users\afsal\OneDrive\Desktop\JailMeet2.0\frontend

Commands:

npm run typecheck
npm run build

If the build only encounters the known Windows sandbox spawn EPERM issue, rerun the same command in the approved environment without modifying code.

Final report must include:

1. Files modified
2. Admin service functions used
3. Officer service functions used
4. Prisoner service functions used
5. Exact fields preserved for each dashboard
6. Confirmation that UI/layout/classes/assets were unchanged
7. Confirmation of no-token, 401, 403, loading, and error behavior
8. Typecheck result
9. Build result
10. Any follow-up risks found

Stop after completing these three dashboard refactors.

Do not continue to hooks, CSS cleanup, component moving, or new workflow pages without explicit confirmation.