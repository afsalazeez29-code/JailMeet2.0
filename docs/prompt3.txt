Proceed with Officer Dashboard Migration Phase 3: Convert officer dashboard content and connect the real backend API.

Project: C:\Users\afsal\OneDrive\Desktop\JailMeet2.0

Current state (already working, do not touch):
- Backend fully working: auth, RBAC, dashboard APIs — all tested
- Officer layout shell already converted and confirmed working (Phase 2 complete):
  - components/legacy/officer/OfficerLayout.tsx
  - components/legacy/officer/OfficerNavbar.tsx
  - components/legacy/officer/OfficerSidebar.tsx
  - components/legacy/officer/OfficerFooter.tsx
  - app/officer/layout.tsx
- /officer redirects to /officer/dashboard, showing the DeskApp shell with placeholder content
- public/legacy/officer is already cleaned (no PHP/env/sql files) and officer.png is confirmed present
- typecheck and build both passing as of Phase 2

Source:
C:\Users\afsal\OneDrive\Desktop\JailMeet2.0\frontend\officer\index.php

Reference doc:
C:\Users\afsal\OneDrive\Desktop\JailMeet2.0\docs\12-officer-dashboard-ui-migration-plan.md

Target:
C:\Users\afsal\OneDrive\Desktop\JailMeet2.0\frontend\app\officer\dashboard\page.tsx

Backend API:
GET http://localhost:5000/api/dashboard/officer
(Protected route — requires Authorization: Bearer <token> header)

IMPORTANT — use these exact backend field names, do not guess or rename them:
- totalPrisoners
- pendingAppointments
- approvedAppointments
- rejectedAppointments
- pendingParoleRequests

Note: the field is approvedAppointments, NOT acceptedAppointments.

Goal:
Preserve the old officer dashboard content UI/UX as closely as possible, replacing old PHP/MySQL dashboard values with the new backend API.

Rules:

1. Convert only officer dashboard content — do not touch prisoners page, appointments page, parole page, FIR page, profile page, or any other officer page.
2. Use the existing OfficerLayout from app/officer/layout.tsx — do not duplicate navbar/sidebar/footer markup inside this page.
3. Remove all PHP session checks, MySQL queries, includes, redirects, and echo blocks from the old index.php content.
4. Make the dashboard page (or a client sub-component within it) a client component, since it fetches using the stored JWT token.
5. Read the JWT token using the SAME auth helper/localStorage key already established during the /login page work — do not invent a second storage mechanism.
6. If no token is found, redirect to /login immediately, before attempting any fetch.
7. Call GET ${NEXT_PUBLIC_API_BASE_URL}/dashboard/officer with the Authorization header.
8. Optionally also call GET ${NEXT_PUBLIC_API_BASE_URL}/auth/me to confirm the logged-in user's identity — this is secondary to the dashboard stats call.
9. If the API response is 401: clear the stored token and redirect to /login.
10. If the API response is 403: show a clear "Access denied" message instead of attempting to render dashboard content.
11. Show a loading state while the fetch is in progress: "Loading officer dashboard..." — never leave a blank screen.
12. Handle fetch/network errors safely with a visible error message — do not let the page crash silently.
13. Missing stat fields should default to 0 rather than showing "undefined" or crashing.
14. Preserve the old DeskApp card layout, classes, icons, and visual style as closely as possible — same structure, just wired to real data instead of PHP/MySQL output.
15. Use only public asset paths under /legacy/officer/... if any are needed on this page.
16. Do NOT load old dashboard JS, ApexCharts, DataTables, jQuery, or analytics scripts for this page — the layout shell already handles necessary interactivity. This page should be data-driven React, not reliant on legacy scripts.
17. Do not modify the backend.
18. Do not delete or move old PHP files.
19. Run:
    npm run typecheck
    npm run build
    Both must pass with zero errors.

After completion, report:
- Files created/modified
- API endpoint(s) used
- Exact backend fields wired (confirm all five: totalPrisoners, pendingAppointments, approvedAppointments, rejectedAppointments, pendingParoleRequests)
- Assets used and any still missing
- Confirmation of token/401/403/loading/error handling behavior
- Any old PHP logic removed
- Final npm run typecheck result
- Final npm run build result