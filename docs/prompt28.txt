You are working on:

C:\Users\afsal\OneDrive\Desktop\JailMeet2.0\frontend

Goal:
Replace the three existing `BsAsterisk` separator icons in `frontend/app/page.tsx` with `IoIosArrowForward`.

Update the import:

Remove:
import { BsAsterisk } from "react-icons/bs";

Add:
import { IoIosArrowForward } from "react-icons/io";

Replace all three `BsAsterisk` components used between:

- Easy Scheduling
- Parole Assistance
- Secure & Reliable

With:

<IoIosArrowForward
  aria-hidden="true"
  style={{
    color: "#ff4a17",
    fontSize: "11px",
    margin: "0 5px",
    verticalAlign: "middle",
    flexShrink: 0,
  }}
/>

Requirements:

- Use exactly `IoIosArrowForward` from `react-icons/io`.
- Color must be exactly `#ff4a17`.
- Keep the icon small and inline with the text.
- Remove the unused `BsAsterisk` import.
- Do not change unrelated text, layout, CSS, or other icons.
- Do not reinstall `react-icons` because it is already installed.

After the change, run:

npm run typecheck
npm run build

Confirm that all three arrow icons render correctly and no unused import remains.