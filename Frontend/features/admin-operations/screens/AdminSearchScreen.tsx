'use client';
import Link from 'next/link'; import { useSearchParams } from 'next/navigation'; import { FormEvent, useEffect, useState } from 'react';
import { ErrorAlert, LoadingAlert } from '@components/common/StatusAlert'; import { searchAdmin, type SearchData } from '../services/admin-operations.service'; import styles from '../components/AdminOperations.module.css';
export default function AdminSearchScreen(){const params=useSearchParams();const [q,setQ]=useState(params.get('q')||'');const [submitted,setSubmitted]=useState(q);const [data,setData]=useState<SearchData|null>(null);const [loading,setLoading]=useState(false);const [error,setError]=useState<string|null>(null);useEffect(()=>{if(submitted.trim().length<2){setData(null);return;}setLoading(true);void searchAdmin(submitted.trim()).then(setData).catch(()=>setError('Search could not be completed.')).finally(()=>setLoading(false));},[submitted]);const submit=(event:FormEvent)=>{event.preventDefault();setError(null);setSubmitted(q)};return <div className={styles.page}><div className={styles.header}><div><h1>Admin Search</h1><p className={styles.muted}>Safe public identities and operational references</p></div></div><form className={styles.filters} onSubmit={submit}><label className="visually-hidden" htmlFor="admin-search-page">Search records</label><input className="form-control" id="admin-search-page" minLength={2} maxLength={100} onChange={(e)=>setQ(e.target.value)} placeholder="VIS-, OFR-, PRN-, workflow reference or name" value={q}/><button className="btn btn-primary" type="submit">Search</button></form>{error?<ErrorAlert>{error}</ErrorAlert>:null}{loading?<LoadingAlert>Searching…</LoadingAlert>:null}{!loading&&data&&!data.groups.length?<p className={styles.card}>No safe matching records.</p>:null}{data?.groups.map((group)=><section className={`${styles.card} mb-3`} key={group.type}><h2>{group.type}</h2>{group.items.map((item)=><Link className="d-block border-top py-2 text-decoration-none" href={item.href} key={item.reference}><strong>{item.title}</strong><span className="d-block text-muted">{item.reference} · {item.subtitle}</span></Link>)}</section>)}</div>}
