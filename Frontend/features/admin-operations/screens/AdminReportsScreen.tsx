'use client';
import { FormEvent, useEffect, useState } from 'react'; import { ErrorAlert, LoadingAlert } from '@components/common/StatusAlert'; import { getReports } from '../services/admin-operations.service'; import styles from '../components/AdminOperations.module.css';
const readable=(value:unknown)=>typeof value==='object'&&value!==null?JSON.stringify(value,null,2):String(value??'—');
export default function AdminReportsScreen(){const [from,setFrom]=useState('');const [to,setTo]=useState('');const [data,setData]=useState<Record<string,unknown>|null>(null);const [loading,setLoading]=useState(false);const [error,setError]=useState<string|null>(null);const load=async()=>{setLoading(true);try{setData(await getReports(from||undefined,to||undefined));setError(null)}catch{setError('Reports are unavailable for the selected range.')}finally{setLoading(false)}};useEffect(()=>{void load()},[]);const submit=(e:FormEvent)=>{e.preventDefault();void load()};return <div className={styles.page}><div className={styles.header}><div><h1>Reports & Analytics</h1><p className={styles.muted}>Aggregated operational data only</p></div></div><form className={styles.filters} onSubmit={submit}><label>From<input className="form-control" type="date" value={from} onChange={(e)=>setFrom(e.target.value)}/></label><label>To<input className="form-control" type="date" value={to} onChange={(e)=>setTo(e.target.value)}/></label><button className="btn btn-primary align-self-end" type="submit">Apply range</button></form>{error?<ErrorAlert>{error}</ErrorAlert>:null}{loading?<LoadingAlert>Calculating reports…</LoadingAlert>:null}{data?<div className={styles.grid}>{Object.entries(data).filter(([key])=>key!=='range').map(([key,value])=><article className={styles.card} key={key}><h2 className="h5">{key.replace(/([A-Z])/g,' $1')}</h2><pre className="mb-0" style={{whiteSpace:'pre-wrap',overflowWrap:'anywhere'}}>{readable(value)}</pre></article>)}</div>:null}</div>}
